# 本地相册功能测试流程

## 测试环境准备

### 1. 构建应用
```bash
npm run build
```

### 2. 启动开发服务器
```bash
npm run tauri dev
```

## 测试场景

### 场景一：本地文件夹选择与导入

#### 步骤 1：打开相册管理
1. 点击顶部标题栏左侧应用名称"给爱的你"
2. 点击左侧菜单中的"相册管理"
3. 确认弹出窗口正常显示

#### 步骤 2：添加本地相册
1. 在相册管理弹窗中，点击"添加相册"按钮
2. 选择"本地相册"选项
3. 点击"选择文件夹"按钮
4. **预期结果**：弹出文件夹选择对话框（使用 macOS 原生文件选择器）
5. 选择一个包含图片的文件夹（如：`/Volumes/Macintosh HD/code/innovate/photo-album/public/jay-chou/photos/`）
6. **预期结果**：
   - 文件夹路径显示在输入框中
   - 显示"预览中..."状态
   - 控制台输出：`[AlbumManager] ✅ 选中文件夹: <路径>`

#### 步骤 3：验证照片导入
1. 输入相册名称（如："周杰伦精选"）
2. 点击"创建"按钮
3. **预期结果**：
   - 控制台输出：`[Tauri] select_folder 命令被调用`
   - 控制台输出：`[Tauri] 选中文件夹："<路径>"`
   - 控制台输出：`[Tauri] 读取文件夹：<路径>`
   - 控制台输出：`[AlbumManager] ✅ 创建本地相册: <名称>`
   - 相册列表中显示新创建的相册
   - 相册类型显示为"本地文件夹"
   - 照片数量正确显示

#### 步骤 4：验证照片信息
1. 点击新创建的相册
2. 在右侧预览区域查看照片列表
3. **预期结果**：
   - 所有支持的图片格式都被识别（jpg, jpeg, png, gif, webp, bmp, heic, heif）
   - 每张照片显示正确的名称、尺寸
   - 照片缩略图正常加载
   - 控制台输出：`[Tauri] 读取文件夹成功：<数量> 张图片`

### 场景二：本地相册操作

#### 测试 1：照片收藏功能
1. 在本地相册中，悬停在一张照片上
2. 点击红色心形图标（收藏）
3. **预期结果**：
   - 心形图标变为红色填充
   - 控制台输出：`[AlbumManager] ❤️ 切换收藏：<照片名> 收藏`
   - 状态正确保存

4. 再次点击心形图标（取消收藏）
5. **预期结果**：
   - 心形图标恢复为灰色轮廓
   - 控制台输出：`[AlbumManager] ❤️ 切换收藏：<照片名> 取消收藏`

#### 测试 2：从相册移除
1. 悬停在一张照片上
2. 点击黄色减号图标（从相册移除）
3. **预期结果**：
   - 控制台输出：`[AlbumManager] ➖ 从相册移除照片: <ID>`
   - 照片从当前相册中移除
   - 照片数量减少

#### 测试 3：永久删除
1. 悬停在一张照片上
2. 点击红色垃圾桶图标（永久删除）
3. **预期结果**：
   - 弹出确认对话框：`"<照片名>" 是本地照片，确定要永久删除吗？`
   - 点击确认后，控制台输出：`[AlbumManager] ❌ 永久删除照片: <照片名>`
   - 照片从系统中删除

#### 测试 4：相册删除
1. 在左侧相册列表中，悬停在一个本地相册上
2. 点击红色垃圾桶图标
3. **预期结果**：
   - 弹出确认对话框：`确定要删除相册"<相册名>"吗？\n注意：这只是删除相册，不会删除本地文件夹中的照片。`
   - 点击确认后，相册从列表中移除
   - 控制台输出：`[AlbumManager] ❌ 删除相册: <相册名>`

### 场景三：多相册管理

#### 测试 1：切换相册
1. 创建多个本地相册
2. 点击不同的相册
3. **预期结果**：
   - 右侧预览区域显示对应相册的照片
   - 顶部标题显示相册名称和照片数量
   - 当前选中的相册高亮显示

#### 测试 2：全部照片视图
1. 点击顶部 Tab 中的"全部"
2. **预期结果**：
   - 显示所有相册中的照片（包括本地相册和虚拟相册）
   - 照片数量统计正确
   - 可以正常滚动（如果照片数量超过一屏）

#### 测试 3：收藏视图
1. 先收藏几张照片（来自不同相册）
2. 点击顶部 Tab 中的"收藏"
3. **预期结果**：
   - 只显示已收藏的照片
   - 照片数量统计正确
   - 可以正常进行收藏/取消收藏操作

### 场景四：电子相册模式

#### 测试 1：幻灯片播放
1. 选择一个本地相册
2. 点击顶部菜单中的"轮播控制"
3. 启用轮播功能
4. 设置播放间隔（如：3 秒）
5. 点击"确定"
6. **预期结果**：
   - 开始幻灯片播放
   - 图片按设置的时间间隔切换
   - 底部进度条正常显示
   - 控制台输出：`[AlbumView] 🔄 自动播放：下一张`

#### 测试 2：方向过滤
1. 在设置面板中，选择"方向" → "横向"
2. **预期结果**：
   - 只显示横向照片（宽高比 >= 1）
   - 照片数量更新
   - 控制台输出：`[AlbumStore] 🔄 切换方向：auto -> landscape`

3. 选择"纵向"
4. **预期结果**：
   - 只显示纵向照片（宽高比 < 1）

5. 选择"自动"
6. **预期结果**：
   - 显示所有照片

### 场景五：性能测试

#### 测试 1：大量照片加载
1. 选择一个包含大量照片（如 100+）的文件夹
2. 创建本地相册
3. **预期结果**：
   - 相册创建时间合理（< 5 秒）
   - 照片列表正常显示
   - 滚动流畅
   - 内存使用合理

#### 测试 2：图片缓存
1. 在网格视图中滚动浏览大量照片
2. **预期结果**：
   - 缩略图优先加载
   - 高清图后台加载
   - 已加载的图片不会重复请求
   - 控制台输出：`[PhotoItem] 缩略图加载成功/高清图加载成功/加载超时`

### 场景六：错误处理

#### 测试 1：无效文件夹路径
1. 手动输入一个不存在的文件夹路径
2. 尝试创建相册
3. **预期结果**：
   - 显示错误提示：`文件夹不存在或无法访问`
   - 相册创建失败

#### 测试 2：空文件夹
1. 选择一个不包含任何图片的文件夹
2. 尝试创建相册
3. **预期结果**：
   - 显示提示：`该文件夹中没有支持的图片文件`
   - 相册创建失败或显示为空相册

#### 测试 3：权限问题
1. 选择一个需要权限的文件夹（如系统文件夹）
2. **预期结果**：
   - macOS 弹出权限请求对话框
   - 授权后可以正常访问
   - 拒绝授权时显示错误提示

## 已知问题和注意事项

### 1. 路径格式
- macOS 路径格式：`/Volumes/Macintosh HD/path/to/folder`
- 确保路径中包含空格时正确处理

### 2. 图片格式支持
支持的格式：jpg, jpeg, png, gif, webp, bmp, heic, heif

### 3. 性能优化
- 缩略图优先加载策略
- 图片缓存机制
- 虚拟滚动（如需要）

### 4. 权限配置
在 `src-tauri/tauri.conf.json` 中需要配置：
```json
"permissions": [
  "core:default",
  "core:window:default",
  "core:window:allow-close",
  "core:window:allow-minimize",
  "core:window:allow-maximize",
  "core:window:allow-toggle-maximize"
]
```

## 测试检查清单

- [ ] 文件夹选择对话框正常弹出
- [ ] 支持的图片格式正确识别
- [ ] 照片信息（名称、尺寸、宽高比）正确读取
- [ ] 缩略图正常生成和显示
- [ ] 收藏功能正常工作
- [ ] 从相册移除功能正常工作
- [ ] 永久删除功能正常工作（带确认对话框）
- [ ] 相册删除功能正常工作
- [ ] 多相册切换正常
- [ ] 全部照片视图正常
- [ ] 收藏视图正常
- [ ] 幻灯片播放正常
- [ ] 方向过滤正常
- [ ] 大量照片加载性能可接受
- [ ] 图片缓存机制有效
- [ ] 错误处理正确（无效路径、空文件夹、权限问题）
- [ ] 控制台日志输出清晰
- [ ] UI 响应流畅

## 测试报告模板

```markdown
## 测试日期：YYYY-MM-DD
## 测试人员：XXX

### 环境信息
- 操作系统：macOS XXX
- 应用版本：vX.X.X
- 浏览器：XXX

### 测试结果
| 测试场景 | 状态 | 备注 |
|---------|------|------|
| 文件夹选择 | ✅/❌ | |
| 照片导入 | ✅/❌ | |
| 收藏功能 | ✅/❌ | |
| 删除功能 | ✅/❌ | |
| 多相册管理 | ✅/❌ | |
| 电子相册 | ✅/❌ | |
| 性能测试 | ✅/❌ | |
| 错误处理 | ✅/❌ | |

### 发现的问题
1. [描述]
2. [描述]

### 建议改进
1. [建议]
2. [建议]
```

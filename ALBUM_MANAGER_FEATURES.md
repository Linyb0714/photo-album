# 相册管理功能说明

## 🎯 核心功能

### 1. **多相册管理**
- ✅ 创建多个自定义相册
- ✅ 自定义相册名称和描述
- ✅ 编辑相册名称
- ✅ 删除相册（不影响照片本身）
- ✅ 相册列表实时显示照片数量

### 2. **相册类型**
- **虚拟相册**：手动添加照片的自定义相册
- **文件夹相册**：从本地文件夹导入（待 Tauri 实现）

### 3. **照片管理**
#### 添加到相册
- ✅ 通过 URL 添加照片
- ✅ 自定义照片名称、尺寸
- ✅ 自动计算缩略图 URL
- ✅ 自动设置相册封面（第一张照片）

#### 照片操作
- ✅ **从相册移除**：仅从当前相册移除，不删除照片
- ✅ **永久删除**：从所有相册和收藏中删除
- ✅ **收藏/取消收藏**：一键切换收藏状态
- ✅ **悬停操作**：鼠标悬停显示操作按钮

### 4. **视图模式**
#### 相册列表视图
- 显示所有相册
- 点击选中相册
- 显示照片数量
- 快捷编辑/删除

#### 当前相册视图
- 3 列照片网格
- 显示照片缩略图
- 悬停显示操作按钮
- 空相册提示

#### 全部照片视图
- 4 列照片网格
- 搜索功能
- 显示所有照片（跨相册）

#### 收藏视图
- 4 列照片网格
- 仅显示收藏照片
- 搜索功能

### 5. **搜索功能**
- 按照片名称搜索
- 实时过滤结果
- 支持全部照片和收藏视图

## 📋 使用流程

### 创建相册
1. 点击"📁 相册管理"打开管理界面
2. 在"相册列表"标签页点击"+ 创建相册"
3. 输入相册名称（必填）
4. 输入描述（可选）
5. 点击"创建"按钮
6. 自动切换到新相册

### 添加照片到相册
1. 选择一个相册
2. 点击"+ 添加照片"
3. 输入照片信息：
   - 照片名称（如：范特西专辑封面）
   - 图片 URL（如：https://picsum.photos/id/28/1920/1080）
   - 宽度（默认 1920）
   - 高度（默认 1080）
4. 点击"添加"按钮
5. 照片自动添加到相册并设为封面（如果是第一张）

### 管理照片
#### 从相册移除
- 悬停照片
- 点击"➖"按钮
- 确认移除
- 照片保留在其他相册和全部照片中

#### 永久删除
- 悬停照片
- 点击"🗑️"按钮
- 确认删除（收藏照片会额外提示）
- 照片从所有相册和收藏中删除

#### 切换收藏
- 点击照片或"❤️"按钮
- 收藏状态立即切换
- 可在"收藏"视图中查看所有收藏

### 编辑相册
1. 悬停在相册卡片上
2. 点击"✏️"编辑按钮
3. 修改相册名称
4. 按 Enter 保存或 Esc 取消

### 删除相册
1. 悬停在相册卡片上
2. 点击"🗑️"删除按钮
3. 确认删除
4. 相册被删除，照片保留

## 🎨 界面布局

```
┌─────────────────────────────────────────────────────┐
│                    相册管理                          │
├──────────────┬──────────────────────────────────────┤
│  相册列表    │           当前相册内容               │
│              │                                      │
│ + 创建相册   │  ┌────────────────────────────────┐  │
│              │  │ 相册名称                       │  │
│ 📁 专辑封面  │  │ 15 张照片                      │  │
│ 📁 演唱会    │  │                    [+ 添加照片]│  │
│ 📁 生活照    │  └────────────────────────────────┘  │
│              │                                      │
│ 全部照片     │  ┌────┐ ┌────┐ ┌────┐              │
│              │  │ 📷 │ │ 📷 │ │ 📷 │              │
│ 收藏         │  └────┘ └────┘ └────┘              │
│              │  ┌────┐ ┌────┐ ┌────┐              │
│              │  │ 📷 │ │ 📷 │ │ 📷 │              │
│              │  └────┘ └────┘ └────┘              │
└──────────────┴──────────────────────────────────────┘
```

## 🔧 技术实现

### 数据结构
```typescript
interface Album {
  id: string;              // 唯一 ID
  name: string;            // 相册名称
  description?: string;    // 相册描述
  type: 'folder' | 'virtual'; // 相册类型
  path?: string;           // 文件夹路径（仅文件夹相册）
  photoIds: string[];      // 照片 ID 列表
  createdAt: Date;         // 创建时间
  updatedAt: Date;         // 更新时间
  coverPhotoId?: string;   // 封面照片 ID
}

interface Photo {
  id: string;              // 唯一 ID
  path: string;            // 图片路径/URL
  name: string;            // 照片名称
  width: number;           // 宽度
  height: number;          // 高度
  aspectRatio: number;     // 宽高比
  createdAt: Date;         // 创建时间
  thumbnail?: string;      // 缩略图 URL
  isFavorite?: boolean;    // 是否收藏
}
```

### 状态管理 (Pinia Store)
```typescript
const photos = ref<Photo[]>([]);      // 所有照片
const albums = ref<Album[]>([]);      // 所有相册
const currentAlbumId = ref<string | null>(null); // 当前相册

// 核心方法
createAlbum(album: Album)
deleteAlbum(albumId: string)
updateAlbum(albumId: string, updates: Partial<Album>)
addPhotoToAlbum(albumId: string, photoId: string)
removePhotoFromAlbum(albumId: string, photoId: string)
addPhoto(photo: Photo)
removePhoto(photoId: string)
toggleFavorite(photoId: string, newStatus?: boolean)
```

## 📊 日志输出

### 创建相册
```
[AlbumStore] 📁 创建相册：周杰伦专辑封面
[AlbumManager] ✅ 创建相册：周杰伦专辑封面
```

### 添加照片
```
[AlbumStore] ➕ 添加照片：范特西专辑封面 | ID: photo-xxx
[AlbumStore]   尺寸：1920 x 1080 | 路径：https://...
[AlbumStore] ➕ 添加照片到相册：photo-xxx -> 周杰伦专辑封面
[AlbumManager] ✅ 添加照片到相册：范特西专辑封面 -> 周杰伦专辑封面
```

### 删除操作
```
[AlbumStore] ➖ 从相册移除照片：photo-xxx from 周杰伦专辑封面
[AlbumManager] ➖ 从相册移除照片：photo-xxx

[AlbumStore] ➖ 删除照片：photo-xxx | 收藏模式：false
[AlbumStore]   ✅ 照片已从主列表删除
[AlbumManager] 💥 永久删除照片：范特西专辑封面
```

## 🚀 后续计划

### 本地文件夹支持 (Tauri)
- [ ] 选择本地文件夹创建相册
- [ ] 自动扫描文件夹中的图片
- [ ] 读取图片元数据（EXIF）
- [ ] 自动生成缩略图
- [ ] 支持图片格式：JPG, PNG, WebP, GIF

### 高级功能
- [ ] 拖拽照片到相册
- [ ] 批量添加/删除照片
- [ ] 相册排序
- [ ] 照片排序
- [ ] 相册封面自定义
- [ ] 导出相册为 ZIP

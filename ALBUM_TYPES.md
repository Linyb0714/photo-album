# 相册类型说明

## 📋 两种相册类型

### 1. 🌐 线上相册 (Virtual Album)
**特点：**
- 手动添加在线图片 URL
- 完全由用户控制
- 支持自定义照片信息（名称、尺寸）
- 适合网络图片、在线资源

**使用方法：**
1. 创建相册时选择"线上相册"
2. 输入相册名称和描述
3. 创建后点击"+ 添加照片"
4. 输入照片信息：
   - 照片名称
   - 图片 URL（如：https://picsum.photos/id/28/1920/1080）
   - 宽度、高度

**适用场景：**
- 在线专辑封面
- 网络收藏图片
- 社交媒体图片
- 在线图库资源

---

### 2. 📁 本地相册 (Folder Album)
**特点：**
- 绑定本地文件夹
- 自动扫描文件夹中的图片
- 读取图片元数据（宽度、高度、创建时间）
- 文件夹内容变化自动更新

**使用方法：**
1. 创建相册时选择"本地相册"
2. 点击"选择"按钮选择本地文件夹
3. 系统自动扫描并导入所有图片
4. 修改文件夹内容后，刷新页面自动更新

**适用场景：**
- 本地照片收藏
- 相机拍摄照片
- 下载的图片集
- 项目素材文件夹

---

## 🎨 界面标识

### 相册列表显示
```
📁 周杰伦专辑封面 [线上]  ← 蓝色标签
📁 旅行照片 [本地]        ← 绿色标签
  📁 /Users/xxx/Pictures  ← 显示文件夹路径
```

### 相册内容页
```
周杰伦专辑封面 [🌐 线上相册]
旅行照片 [📁 本地相册]
  📁 /Users/xxx/Pictures/旅行
```

---

## 🔧 技术实现

### 数据结构
```typescript
interface Album {
  id: string;
  name: string;
  description?: string;
  type: 'folder' | 'virtual';  // folder=本地，virtual=线上
  path?: string;               // 仅本地相册有
  photoIds: string[];
  createdAt: Date;
  updatedAt: Date;
  coverPhotoId?: string | null;
}
```

### 核心流程

#### 创建线上相册
```
用户输入 → 创建 Album(type='virtual') → 保存到 store → 等待手动添加照片
```

#### 创建本地相册
```
用户选择文件夹 → 创建 Album(type='folder', path='/xxx') 
  → 调用 readFolder(path) 
  → 扫描图片文件 
  → 读取元数据 
  → 自动添加照片到相册
```

#### 添加照片
```
线上相册:
  用户输入 URL → 创建 Photo → 添加到相册

本地相册:
  ❌ 不支持手动添加
  ✅ 修改文件夹内容后刷新自动同步
```

---

## 📊 功能对比

| 功能 | 线上相册 | 本地相册 |
|------|---------|---------|
| 创建方式 | 输入名称 | 选择文件夹 |
| 添加照片 | 手动输入 URL | 自动扫描 |
| 删除照片 | ✅ 支持 | ✅ 支持（从相册移除） |
| 编辑照片信息 | ✅ 支持 | ⚠️ 仅读取元数据 |
| 实时更新 | ❌ 手动添加 | ✅ 刷新页面自动同步 |
| 依赖网络 | ✅ 需要 | ❌ 不需要 |
| 存储位置 | 云端/网络 | 本地硬盘 |

---

## 🚀 使用示例

### 示例 1：创建周杰伦专辑线上相册
1. 点击"📁 相册管理"
2. 点击"+ 创建相册"
3. 输入名称："周杰伦专辑封面"
4. 选择类型："🌐 线上相册"
5. 输入描述："收录 20 张经典专辑"
6. 点击"创建"
7. 点击"+ 添加照片"
8. 输入：
   - 名称：范特西专辑
   - URL: https://picsum.photos/id/28/1920/1080
   - 宽度：1920
   - 高度：1080
9. 点击"添加"

### 示例 2：创建旅行照片本地相册
1. 点击"📁 相册管理"
2. 点击"+ 创建相册"
3. 输入名称："2024 旅行照片"
4. 选择类型："📁 本地相册"
5. 点击"选择"按钮
6. 选择文件夹：`/Users/xxx/Pictures/旅行/2024`
7. 点击"创建"
8. 系统自动扫描并导入所有照片
9. 查看相册内容

---

## ⚠️ 注意事项

### 线上相册
- 图片 URL 必须有效
- 建议使用支持缓存的图片服务
- 网络问题可能导致加载失败

### 本地相册
- **需要 Tauri 后端支持**（Web 环境无法使用）
- 文件夹路径不能包含特殊字符
- 仅支持常见图片格式（JPG, PNG, WebP）
- 大文件夹扫描可能需要时间
- 删除照片不会删除本地文件

---

## 🔮 后续计划

### 本地相册增强
- [ ] 实时监听文件夹变化（无需刷新）
- [ ] 支持更多图片格式（GIF, SVG, TIFF）
- [ ] 读取 EXIF 信息（拍摄时间、相机参数）
- [ ] 生成缩略图缓存
- [ ] 支持子文件夹递归扫描
- [ ] 智能分类（按日期、地点）

### 混合相册
- [ ] 支持同时包含线上和本地照片
- [ ] 智能去重
- [ ] 照片同步功能

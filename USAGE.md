# 相册应用 - 使用指南

## 🎯 应用概述

这是一个现代化的**桌面端跨平台相册应用**，具有独特的交互设计和视觉效果。

### 主要特点

1. **智能交互**
   - 点击任意位置唤醒菜单
   - 菜单智能贴边显示
   - 12 秒自动关闭
   - 悬停 3 秒触发 3D 效果

2. **视觉效果**
   - 6 种精美相框模版
   - 马赛克碎片化 3D 效果
   - 明显的阴影和浮动动画
   - 鼠标跟随视差效果

3. **桌面端特性**
   - 原生文件系统访问
   - 自动扫描照片文件夹
   - 支持多种图片格式
   - 跨平台（macOS/Windows/Linux）

## 📖 详细使用说明

### 首次使用

#### 1. 启动应用
```bash
# 开发模式
npm run tauri dev

# 或使用已构建的应用
# macOS: 打开 src-tauri/target/release/bundle/dmg/PhotoAlbum.dmg
# Windows: 打开 src-tauri/target/release/bundle/msi/PhotoAlbum.msi
# Linux: 打开 src-tauri/target/release/bundle/deb/photo-album.deb
```

#### 2. 添加相册
- 应用启动后，点击页面任意位置
- 在弹出的菜单中点击"添加照片"
- 选择包含照片的文件夹
- 应用会自动扫描并加载所有图片

**支持的图片格式**：
- JPG/JPEG
- PNG
- GIF
- WebP
- BMP
- HEIC/HEIF

#### 3. 查看照片
- 默认显示网格布局
- 照片以彩色方块形式展示（开发版）
- 实际应用中会显示真实图片
- 支持 6 种不同的相框模版

### 功能菜单

点击页面任意位置可打开功能菜单：

```
┌─────────────────────┐
│   相册功能菜单      │
├─────────────────────┤
│  ➕ 添加照片        │ ← 选择新的相册文件夹
│  ➖ 删除照片        │ ← 删除当前照片
│  ◀ 上一张          │ ← 切换到上一张
│  ▶ 下一张          │ ← 切换到下一张
│  ⏯ 开始轮播        │ ← 开启/暂停轮播
│  ⛶ 全屏            │ ← 进入/退出全屏
│  ⬇ 导出            │ ← 导出为 GIF/MP4/WebM
│  ⚙ 设置            │ ← 打开设置面板
│  ▣ 切换模版        │ ← 选择相框样式
└─────────────────────┘
```

### 相框模版

#### 1. 网格相框（默认）
- 3x2 规则网格布局
- 适合尺寸相近的照片
- 整齐美观

#### 2. 瀑布流
- Masonry 布局
- 竖图和横图完美混合
- 类似 Pinterest 效果

#### 3. 拼贴相框
- 智能 bin-packing 算法
- 自动拼接不同尺寸照片
- 最大化利用空间

#### 4. 时间轴
- 按拍摄时间排序
- 线性排列
- 适合回忆展示

#### 5. 画廊
- 大图主图 + 缩略图
- 突出主要照片
- 经典画廊效果

#### 6. 杂志
- 不规则艺术排版
- 类似杂志封面
- 创意展示

### 3D 悬停效果

**触发条件**：
- 鼠标在相册上静止悬停 3 秒

**效果表现**：
1. 照片间距增大（马赛克碎片化）
2. 照片向上浮起
3. 明显深阴影
4. 上下浮动动画
5. 鼠标跟随视差

**退出条件**：
- 鼠标移动
- 移动 1 秒后自动恢复

### 设置面板

打开菜单后点击"设置"：

#### 相框模版
- 选择 6 种相框之一
- 实时预览效果

#### 轮播设置
- **自动轮播**：开启/关闭
- **切换间隔**：
  - 3 秒
  - 5 秒
  - 10 秒
  - 30 秒
  - 1 分钟
- **切换动画**：
  - 淡入淡出
  - 滑动
  - 缩放
  - 立方体

#### 悬停效果
- 开启/关闭 3D 效果
- 说明文字提示

#### 菜单自动关闭
- 8 秒
- 12 秒（默认）
- 20 秒

### 快捷键

| 按键 | 功能 |
|------|------|
| `ESC` | 关闭菜单 |
| `←` | 上一张 |
| `→` | 下一张 |
| `F` | 全屏切换 |
| `Space` | 暂停/继续轮播 |

## 🎨 视觉设计

### 颜色主题
- 深色背景：#111827（gray-900）
- 渐变效果：gray-900 → gray-800 → gray-900
- 强调色：蓝色 #3b82f6

### 动画效果
- 菜单滑入：200-300ms ease-out
- 照片过渡：500ms ease-out
- 浮动动画：3s ease-in-out infinite
- 悬停效果：300ms

### 3D 效果参数
- 缩放：1.1x（焦点）/ 0.95x（周围）
- 阴影：0 25px 50px rgba(0,0,0,0.5)
- 视差范围：±10px
- 浮动幅度：±8px

## 🔧 高级功能

### 批量添加相册
1. 创建包含多个子文件夹的目录
2. 选择主文件夹
3. 应用会递归扫描所有子文件夹

### 常用文件夹检测
应用会自动检测以下文件夹：
- ~/Pictures（图片）
- ~/Desktop（桌面）
- ~/Downloads（下载）
- 其他包含照片的文件夹

### 导出功能（待实现）
- **GIF**：适合短时长、小尺寸
- **MP4**：高质量、压缩好
- **WebM**：现代浏览器支持

## 📱 平台特性

### macOS
- 原生窗口样式
- 触控板手势支持
- 深色模式适配
- Retina 显示优化

### Windows
- 原生窗口样式
- 触摸优化
- 深色模式适配
- 高分屏支持

### Linux
- 原生窗口样式
- 主题适配
- 深色模式
- 多显示器支持

## 🐛 故障排除

### 问题：菜单不弹出
**解决**：检查是否点击了照片区域，尝试点击空白处

### 问题：3D 效果不触发
**解决**：
1. 确保鼠标静止不动
2. 等待 3 秒
3. 检查设置中是否开启了 3D 效果

### 问题：照片加载失败
**解决**：
1. 检查文件夹权限
2. 确认文件夹包含支持的图片格式
3. 重启应用

### 问题：应用卡顿
**解决**：
1. 减少相册中的照片数量
2. 关闭 3D 效果
3. 使用较小的图片

## 📚 开发文档

### 添加新功能

#### 1. 前端组件
- 在 `src/components/` 创建新组件
- 在 `src/composables/` 添加逻辑
- 在 `src/stores/` 管理状态

#### 2. 后端 API
- 在 `src-tauri/src/lib.rs` 添加命令
- 在 `src/utils/tauriApi.ts` 封装调用

#### 3. 类型定义
- 在 `src/types/index.ts` 添加类型

### 构建优化

#### 减小应用体积
```bash
# 启用压缩
npm run tauri build -- --release

# 仅构建当前平台
npm run tauri build -- --target <platform>
```

#### 性能优化
- 启用缩略图缓存
- 懒加载图片
- 虚拟滚动（大量照片时）

## 📞 反馈与支持

如有问题或建议，请：
1. 查看 README.md
2. 查看开发文档
3. 提交 Issue

---

**版本**: 0.1.0  
**更新日期**: 2024-08-24  
**平台**: macOS / Windows / Linux
